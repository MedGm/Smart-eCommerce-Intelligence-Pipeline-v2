apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Cluster-scoped resources only (CRDs, ClusterRoles — no Docker images here).
# Applied separately BEFORE namespace workloads by the deployment script.
resources:
  - ../../../kfp-upstream/cluster-scoped-resources
  - argo-clusterrole.yaml
